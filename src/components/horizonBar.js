export function horizonBarCreate(that, resume,finalWidth,x,y ) {
  // that.outerCircle = resume.add.graphics();
  // that.circle = resume.add.graphics();
  // that.yCoord = y;
  // that.capSize = 5;
  // that.bar = that.add.bitmapData(finalWidth, that.capSize*2);
  // resume.add.sprite(x, y, that.bar);
  // resume.add.tween(that).to({barProgress: that.bar.width-that.capSize*2}, 3000, null, true, 0);

}



export function horizonBarUpdate(that,color) {
  // that.outerCircle.clear();
  // that.outerCircle.beginFill(0xFFFFFF, 0.3);
  // that.outerCircle.drawCircle(that.barProgress+113, that.yCoord+7, that.capSize*10);
  //
  // that.circle.clear();
  // that.circle.beginFill(color[0], 1);
  // that.circle.drawCircle(that.barProgress+113, that.yCoord+7, that.capSize*3);
  //
  //
  // that.bar.context.lineJoin = "round";
  // that.bar.context.lineWidth = that.capSize;
  //
  //
  // that.bar.context.clearRect(0,0, that.bar.width, that.bar.height);
  // that.bar.context.strokeRect(that.capSize*1.5, that.capSize*1.5, that.barProgress, 0);
  // that.bar.context.strokeStyle = color[1];
  //
  //
  // that.bar.dirty = true;
}
